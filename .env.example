PORT=5001
MONGODB_URI=mongodb://127.0.0.1:27017/choice_grid
JWT_SECRET=your_jwt_secret_key_here
CLIENT_URL=http://localhost:5173

# Optional SMTP Settings (falls back to Ethereal test account if omitted)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASS=secret
# EMAIL_FROM="Choice Grid <hello@choicegrid.app>"
